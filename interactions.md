# Interactions: навигация, клавиши, оверлеи

Модель взаимодействия kvn — клавишная, vim-стиль. Источник — `src/ui/help.rs`,
`src/ui/nav.rs`, README.

## 1. Модель фокуса

- Две основные панели: **Profiles** (слева) и **Logs** (справа).
- Фокус хранится в `MainPaneFocus::Sources | Logs`. Активная панель подсвечивает
  рамку акцентом.
- Переключение фокуса: `Ctrl+h`/`←` — предыдущая, `Ctrl+l`/`→` — следующая.
- Выбор внутри списка — `j`/`↓` вниз, `k`/`↑` вверх, `gg`/`G` — в начало/конец.

## 2. Карта клавиш

### Навигация

| Клавиша | Действие |
|---|---|
| `Ctrl+h` / `←` | фокус на предыдущую панель |
| `Ctrl+l` / `→` | фокус на следующую панель |
| `j` / `↓` | вниз / скролл |
| `k` / `↑` | вверх / скролл |
| `gg` / `G` | первый / последний элемент |

### Источники (Profiles)

| Клавиша | Действие |
|---|---|
| `Enter` | подключиться к выбранному профилю |
| `e` | открыть `profiles.json` в `$EDITOR` |
| `y` | скопировать источник |
| `p` | вставить источник из буфера |
| `d` | удалить выбранный источник |
| `u` | обновить выбранную подписку/geo |
| `i` / `I` | цикл авто-обновления подписки / geo |
| `t` / `T` | тест выбранного / всех профилей |

### Логи

| Клавиша | Действие |
|---|---|
| `y` | копировать запись (или visual selection) |
| `Shift+V` | начать визуальное выделение |
| `j`/`k` | расширить выделение на запись |
| `gg`/`G` | расширить к началу/концу буфера |
| `Esc` | снять выделение |

### Подключение

| Клавиша | Действие |
|---|---|
| `r` | переподключиться |
| `s` | отключиться |
| `a` | toggle auto-connect |
| `K` | toggle kill switch |

### Настройки

| Клавиша | Действие |
|---|---|
| `m` | режим роутинга |
| `o` | geo-регион |
| `D` | DNS-настройки |
| `S` | сервис-роутинг |
| `C` | выбор темы |

### Приложение

| Клавиша | Действие |
|---|---|
| `q` / `Esc` | отсоединить TUI (демон и VPN продолжают работать); в диалоге — отмена |
| `Ctrl+C` | остановить демон, отключить VPN, выйти полностью |
| `?` | открыть/закрыть справку |

> На терминалах с Kitty keyboard protocol буквенные шорткаты следуют физическим
> US-раскладкам независимо от активного языка.

## 3. Оверлеи (модалки)

Оверлеи рисуются центрированными поверх основного UI. Пока открыт оверлей,
`q`/`Esc` закрывает его (не выходит из приложения).

| Оверлей | Клавиша | Поведение |
|---|---|---|
| Help | `?` | таблица клавиш (колонка «клавиша» + «действие»), закрывается `?`/`Esc`/`q` |
| ConfirmDelete | `d` | `y` — удалить, `n`/`q`/`Esc` — отмена |
| RoutingMode | `m` | `h`/`l` — выбор, `Enter` — подтвердить, `q`/`Esc` — отмена |
| GeoRegions | `o` | `j`/`k` выбор региона, `h`/`l` режим, `Enter` — подтвердить |
| DnsSettings | `D` | `j`/`k` выбор, `h`/`l` цикл стратегии/факипа, `Enter` — подтвердить |
| ThemeSettings | `C` | `j`/`k` выбор темы (live-превью), `Enter` — применить |
| ServiceRouting | `S` | `h`/`l` цикл Direct/Proxy/Disabled, `Enter` — применить |
| Support / Migration | — | информационные/сервисные |

**Диалоговые клавиши внутри оверлеев:** `h`/`l` (или `←`/`→`) — менять значение,
`Enter` — подтвердить, `y`/`n` — да/нет, `q`/`Esc` — отмена. Активный пункт
оверлея — `success()` (жирный зелёный).

## 4. Тосты (временные уведомления)

- Появляются справа сверху, исчезают автоматически.
- `Info` (акцентная рамка) — «Connected to X», «Imported N profiles» и т.п.
- `Error` (красная рамка) — ошибки подключения/конфигурации, текст переносится.
- Не занимают место в статус-баре (он статичен).

## 5. Состояния подключения (бейджи)

| Состояние | Бейдж | Цвет |
|---|---|---|
| подключено | ` CONNECTED ` | зелёный фон |
| подключение | ` CONNECTING ` | жёлтый фон |
| отключено | ` DISCONNECTED ` | серый фон |

Строка активного профиля в списке при подключении становится зелёной
(`success()`), а если она ещё и выбрана — затемнённо-зелёной
(`selected_connected()`).

## 6. Принципы взаимодействия (для переноса)

1. **Клавиатура первична**, мышь опциональна (оверлеи/кнопки — бонус, не опора).
2. **Vim-семантика**: `j`/`k`/`gg`/`G`, `h`/`l` — переключение панелей/значений.
3. **Один оверлей за раз**; `q`/`Esc` всегда отменяет ближайший уровень.
4. **Обратимость**: `q` отсоединяет TUI, но не трогает фоновый демон/VPN;
   полный выход — только `Ctrl+C`.
5. **Состояние видно всегда**: бейджи в статус-баре + цвет строки подключённого
   профиля, без отдельного «экрана статуса».
6. **Live-фидбек**: тест профиля (`t`) показывает `…` в колонке latency, пока
   идёт проверка.
